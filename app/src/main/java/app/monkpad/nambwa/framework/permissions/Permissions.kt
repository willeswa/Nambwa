package app.monkpad.nambwa.framework.permissions

enum class Permissions(title: String, message: String) {
    USAGE_STATS(
        "Usage Access Permission",
        "This app needs the Usage Access permission to function correctly. Please enable it in the settings."
    )
}